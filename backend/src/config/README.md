# Sequelize Configuration

This directory contains Sequelize database configuration.

## Files

- `env.js` - Environment variables configuration
- `database.js` - Sequelize connection setup
- `sequelize-config.js` - Sequelize CLI configuration (used by .sequelizerc)
