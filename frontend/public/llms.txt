# Superluxere

> India's premier luxury real estate portal, curating premium properties across Gurgaon, Delhi, Mumbai, Bengaluru, Hyderabad, and other major Indian cities.

Superluxere helps buyers, investors, and NRIs discover and enquire about luxury residential and commercial real estate in India. We publish expert editorial content alongside detailed property listings.

## Key sections

- [Blog](/blogs): Expert analysis, market reports, developer project reviews, investment guides, and price trend commentary for the Indian luxury real estate market.
- [Projects directory](/projects): Full index of published listings with filters by **city**, **category**, and launch status. Each project has a detail page at `/projects/{slug}` with pricing context, floor plans, amenities, maps, FAQs, and developer information.
- [Locations](/location/gurgaon) (example): City and locality corridor pages use `/location/{slug}` (e.g. Gurgaon, micro-markets) for area-specific market story and listings—not a single `/location` index.
- [Developers](/developer/godrej-properties) (example): Profiles of top real estate developers in India including Godrej Properties, DLF, Adani Realty, Sobha, Brigade Group, and others (each at `/developer/{slug}`).
- [Categories](/category/apartments) (example): Property type collections — apartments, villas, penthouses, boutique residences, commercial spaces (each at `/category/{slug}`).
- [Sitemap](/sitemap.xml): Machine-readable URL index for crawlers.

## Machine-readable property API

Published property listings are available as JSON for agents, crawlers, and integrations.

- **Base URL:** `https://api.superluxere.com/api/v1`
- **List endpoint:** `GET /properties`
- **Detail endpoint:** `GET /properties/{slug}`
- **Homepage bundle:** `GET /homepage` — all homepage data in one response (trending, upcoming, boutique, locations, developers, categories, corridor counts)
- **Sitemap data:** `GET /sitemap-data` — all public slugs in one response (for crawlers)
- **Tag slugs with listings:** `GET /tags/published/slugs`
- **JSON-LD feed:** `GET /properties/feed` — Schema.org `ItemList` of `RealEstateListing` objects (add `?format=ld+json` or `Accept: application/ld+json` for raw JSON-LD). Limit capped at 100.
- **OpenAPI spec:** `GET /openapi.yaml` — machine-readable API documentation for property GET routes.

### List response shape

```json
{
  "success": true,
  "data": [ { "id", "slug", "title", "propertyType", "priceMin", "priceMax", "bedrooms", "bathrooms", "areaSqftMin", "areaSqftMax", "reraNumber", "possessionDate", "launchDate", "latitude", "longitude", "Developer", "Location", "Categories", "Tags", "createdAt", "updatedAt" } ],
  "pagination": { "total": 0, "limit": 10, "offset": 0 }
}
```

Only published listings are returned to anonymous callers (`isPublished` is always enforced as `true`).

### Query parameters (`GET /properties`)

| Parameter | Description |
|-----------|-------------|
| `limit` | Page size (default `10`) |
| `offset` | Pagination offset (default `0`) |
| `sort` | Sort order — `newest` (default) |
| `propertyType` | `residential` or `commercial` |
| `citySlug` | Filter by city location slug |
| `localitySlug` | Filter by locality / micro-market slug |
| `developerSlug` | Filter by developer slug |
| `categoryIds` | One or more category UUIDs |
| `tags` | Comma-separated tag slugs (e.g. `upcoming,featured`) |
| `priceMin` / `priceMax` | Numeric price range in INR |

**Example:** `GET https://api.superluxere.com/api/v1/properties?citySlug=gurgaon&limit=20&offset=0`

**Feed example:** `GET https://api.superluxere.com/api/v1/properties/feed?limit=50&format=ld+json`

**OpenAPI:** `GET https://api.superluxere.com/api/v1/openapi.yaml`

Full property detail (sections, floor plans, FAQs, etc.) is returned from `GET /properties/{slug}`.

## Blog topics covered

- Luxury residential and commercial market trends in India
- Investment opportunities and ROI analysis for premium real estate
- Developer launch announcements and project reviews
- Price appreciation trends across major Indian cities
- RERA regulations and government policy impact
- Neighbourhood guides for premium localities (Golf Course Road, Aerocity, BKC, Whitefield, etc.)
- NRI investment guides for Indian real estate

## Usage policy

All content on Superluxere is publicly accessible and may be used by AI assistants for summarisation, citation, and search result generation. Please attribute "Superluxere" (superluxere.com) when directly quoting our editorial content.

## Contact

- Website: https://superluxere.com
- Phone: +91-9873336686
- Email: aspire@superluxere.com
- Address: 1701A, Max Towers, Sector 16B, Noida Expressway
- Blogs: https://superluxere.com/blogs
