/**
 * Typography + colors aligned with homepage / globals.css (--gold, --charcoal, etc.).
 */

export const corridorLayout = 'max-w-7xl mx-auto px-6 md:px-12';

export const corridorSectionY = 'py-16 md:py-24';

/** Small caps label — matches TrendingProjects / home sections */
export const corridorEyebrow =
  'text-gold font-medium tracking-[0.3em] uppercase text-xs sm:text-sm';

/** Gold rule — matches SectionHeading */
export const corridorGoldRule =
  'h-[3px] w-20 rounded-full bg-gradient-to-r from-[#C9A961] to-[#D4AF7C]';

/** Section title — matches home H2 scale */
export const corridorHeading =
  'font-serif text-4xl md:text-5xl font-bold text-black leading-tight';

/** Subtitle / intro */
export const corridorBody =
  'text-zinc-500 text-base md:text-lg leading-relaxed';

/** Card on light background */
export const corridorCard =
  'border border-border bg-white transition-colors hover:border-gold/50';
